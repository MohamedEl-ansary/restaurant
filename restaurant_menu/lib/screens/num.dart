int TotalCash = 0;
double increace14 = TotalCash + (TotalCash * 0.14);
double AllCach = 0;

// TotalCash + (TotalCash * 0.14)

  // int TotalCash = 0;
  
  // // Calculate 14% of totalCash
  // double increase = TotalCash * 0.14;
  
  // // Increase totalCash by 14%
  // TotalCash += increase.round();
  
  // // Assign totalCash to allCash
  // double AllCach = TotalCash.toDouble();
  
  // print(AllCach); // Output the increased cash
